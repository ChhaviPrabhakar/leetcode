while(n>0)
        {
            var prev=n&1;
            var  curr=(n>>1)&1;
            if(prev^curr==0)
                {
                    return false;
                }
            n=n>>1;
        }
    return true;
